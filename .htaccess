<IfModule mod_rewrite.c>
    RewriteEngine On

    # Allow direct access to static assets in public/
    RewriteRule ^public/ - [L]

    RewriteRule ^$ public/ [L]
    RewriteRule (.*) public/$1 [L]
</IfModule>
